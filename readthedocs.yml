version: 2

build:
  os: ubuntu-20.04
  tools:
    python: '3.9'
  jobs:
    post_checkout:
      - git config core.ignorecase false
      - git checkout main
      - git checkout update

python:
  install:
    - requirements: docs/sphinx/requirements.txt
    - method: pip
      path: .
